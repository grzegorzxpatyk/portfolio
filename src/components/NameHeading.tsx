import { styled } from '../styles/stitches';

const NameHeading = styled('h1', {
    fontSize: '1.2rem',
    fontWeight: 500,
    lineHeight: 1.5,
    marginBottom: '1rem',
});

export default NameHeading;
